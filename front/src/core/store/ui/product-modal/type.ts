export type UIProfile = {
    userInfo: UIProfileUser;
    memberInfo: UIProfileMember;
};

type UIProfileUser = {
    name: string;
    age: string;
    tel: string;
};

type UIProfileMember = {
    name: string;
    age: string;
    tel: string;
};
